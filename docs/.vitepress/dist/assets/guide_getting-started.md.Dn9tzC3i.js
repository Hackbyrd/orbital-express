import{_ as s,o as a,c as n,a2 as i}from"./chunks/framework.DfwzJIgY.js";const u=JSON.parse('{"title":"Getting Started","description":"","frontmatter":{},"headers":[],"relativePath":"guide/getting-started.md","filePath":"guide/getting-started.md"}'),t={name:"guide/getting-started.md"};function r(l,e,o,p,d,c){return a(),n("div",null,[...e[0]||(e[0]=[i(`<h1 id="getting-started" tabindex="-1">Getting Started <a class="header-anchor" href="#getting-started" aria-label="Permalink to &quot;Getting Started&quot;">​</a></h1><p>Orbital-Express is an opinionated Express.js + Sequelize (PostgreSQL) framework for building really good backend APIs fast. It combines Django&#39;s <strong>feature-based folder structure</strong> with Rails&#39; Model-View-Controller concept, and layers in a background-job system (Bull/Redis) and real-time Socket.IO alongside the web server.</p><p>This guide gets you from zero to a running local environment. If you have not read <a href="./../">What is Orbital-Express?</a> yet, do that first — it explains the mental model before you touch any code.</p><hr><h2 id="prerequisites" tabindex="-1">Prerequisites <a class="header-anchor" href="#prerequisites" aria-label="Permalink to &quot;Prerequisites&quot;">​</a></h2><p>Before you install anything, make sure your machine has:</p><table tabindex="0"><thead><tr><th>Requirement</th><th>Version</th><th>Notes</th></tr></thead><tbody><tr><td><strong>Node.js</strong></td><td>v22.x.x</td><td>Use <a href="../nvm.txt">nvm</a> to pin the version per project</td></tr><tr><td><strong>PostgreSQL</strong></td><td>14+</td><td>Must be running before <code>yarn migrate</code> or <code>yarn test</code></td></tr><tr><td><strong>Redis</strong></td><td>7+</td><td>See <a href="#redis-project-local">Redis setup</a> below — do <strong>not</strong> install system-wide</td></tr><tr><td><strong>Yarn</strong></td><td>1.x (classic)</td><td><code>npm install -g yarn</code></td></tr></tbody></table><div class="tip custom-block"><p class="custom-block-title">Knowledge assumed</p><p>Orbital-Express is not a &quot;hello world&quot; starter. This guide assumes you understand JavaScript (ES6+), Node.js, and the basic request/response lifecycle of an Express app. If any of those feel shaky, read up on them before continuing.</p></div><hr><h2 id="installation" tabindex="-1">Installation <a class="header-anchor" href="#installation" aria-label="Permalink to &quot;Installation&quot;">​</a></h2><h3 id="_1-clone-the-repo-and-install-dependencies" tabindex="-1">1. Clone the repo and install dependencies <a class="header-anchor" href="#_1-clone-the-repo-and-install-dependencies" aria-label="Permalink to &quot;1. Clone the repo and install dependencies&quot;">​</a></h3><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> clone</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">your-orbital-express-rep</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">o</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> my-api</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> my-api</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">yarn</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>All dependencies are pinned to <strong>exact versions</strong> — no <code>^</code> or <code>~</code> ranges anywhere in <code>package.json</code>. Keep it that way.</p><div class="warning custom-block"><p class="custom-block-title">Always pin exact versions</p><p>When you add any new dependency, always pass <code>--exact</code>:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">yarn</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">modul</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">e</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --exact</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">          # regular dependency</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">yarn</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">modul</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">e</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --exact</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --dev</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # dev dependency</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>Range versions (<code>^1.2.3</code>, <code>~1.2.3</code>) let the package manager silently install a different version on a fresh checkout or CI run. Exact pins guarantee every developer, every CI run, and every deploy installs the identical dependency tree.</p></div><hr><h2 id="redis-project-local" tabindex="-1">Redis — Project-Local Setup <a class="header-anchor" href="#redis-project-local" aria-label="Permalink to &quot;Redis — Project-Local Setup {#redis-project-local}&quot;">​</a></h2><p>Orbital-Express does <strong>not</strong> use a system-wide Redis (<code>brew install redis</code> is not what we want here). Each project builds and runs its own copy of Redis inside the repo, pinned to the version the project expects.</p><p>The <code>redis/</code> folder is gitignored (it is a large binary), so every developer installs it once:</p><ol><li>Download a release from <a href="http://download.redis.io/releases/" target="_blank" rel="noreferrer">http://download.redis.io/releases/</a>.</li><li>Build it:<div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> redis-x.x.x</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">make</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">make</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div></li><li>Rename the folder to <code>redis/</code> at the repo root.</li><li>Inside <code>redis/</code>, create a <code>vX.X.X.txt</code> file noting the version — so the team knows which version this project expects.</li><li>Start Redis with:<div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">yarn</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> redis</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div>Stop it with <code>yarn redis:stop</code>.</li></ol><p>Full details are in <a href="../redis.txt">docs/redis.txt</a>. Redis must be running for the worker process, Socket.IO, and the test suite.</p><div class="tip custom-block"><p class="custom-block-title">Local dev only</p><p>The project-local Redis is purely for running the app on your machine. In production, connect to a <strong>managed Redis</strong> (Heroku Redis, Redis Cloud, etc.) via the <code>REDIS_URL</code> config var. You never build Redis from the repo in a deployed environment.</p></div><hr><h2 id="configuration" tabindex="-1">Configuration <a class="header-anchor" href="#configuration" aria-label="Permalink to &quot;Configuration&quot;">​</a></h2><p>All environment variables live in per-environment files under <code>config/</code>. The committed template is <code>config/.env.template</code> — it is the canonical reference for every variable the app accepts.</p><h3 id="create-your-local-env-files" tabindex="-1">Create your local env files <a class="header-anchor" href="#create-your-local-env-files" aria-label="Permalink to &quot;Create your local env files&quot;">​</a></h3><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cp</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> config/.env.template</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> config/.env.development</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cp</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> config/.env.template</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> config/.env.test</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>Fill in the values for each file. The <code>.env.development</code> and <code>.env.test</code> files are gitignored — they never appear in the repository.</p><div class="warning custom-block"><p class="custom-block-title">Use a separate database for tests</p><p><code>DATABASE_URL</code> in <code>.env.test</code> must point to a <strong>different</strong> Postgres database than development. The test suite drops and recreates tables on every run.</p></div><h3 id="required-variables" tabindex="-1">Required variables <a class="header-anchor" href="#required-variables" aria-label="Permalink to &quot;Required variables&quot;">​</a></h3><p>The source of truth is always <code>config/.env.template</code>, but these are the minimum needed to boot:</p><table tabindex="0"><thead><tr><th>Variable</th><th>What it is</th></tr></thead><tbody><tr><td><code>NODE_ENV</code></td><td><code>development</code> / <code>test</code> / <code>production</code> — selects which <code>.env.*</code> loads</td></tr><tr><td><code>DATABASE_URL</code></td><td>Postgres connection string</td></tr><tr><td><code>REDIS_URL</code></td><td>Redis connection (Bull queues + Socket.IO adapter)</td></tr><tr><td><code>ACCESS_TOKEN_SECRET</code></td><td>Signing secret for the short-lived access JWT</td></tr><tr><td><code>REFRESH_TOKEN_SECRET</code></td><td>Signing secret for refresh tokens — must differ from the access secret</td></tr><tr><td><code>ACCESS_TOKEN_EXPIRES_IN</code></td><td>Access-token lifetime, e.g. <code>15m</code></td></tr><tr><td><code>REFRESH_TOKEN_EXPIRES_IN</code></td><td>Refresh-token lifetime, e.g. <code>60d</code></td></tr></tbody></table><div class="warning custom-block"><p class="custom-block-title">All auth vars in every env file</p><p><code>ACCESS_TOKEN_SECRET</code>, <code>REFRESH_TOKEN_SECRET</code>, <code>ACCESS_TOKEN_EXPIRES_IN</code>, and <code>REFRESH_TOKEN_EXPIRES_IN</code> must be present in <strong>every</strong> env file, including <code>.env.test</code>. If they are missing from <code>.env.test</code>, auth tests fail in confusing ways. Keep the two secrets distinct, and never commit real secrets — only <code>.env.template</code> is committed.</p></div><p>Every time you add a new config variable to the app, update <code>.env.template</code> so the next developer who pulls your changes knows it exists.</p><h3 id="how-the-config-loads" tabindex="-1">How the config loads <a class="header-anchor" href="#how-the-config-loads" aria-label="Permalink to &quot;How the config loads&quot;">​</a></h3><p><code>config/config.js</code> is what the Sequelize CLI reads to run migrations — it is pointed at by <code>.sequelizerc</code> at the repo root. It loads the matching <code>.env.&lt;env&gt;</code> by <code>NODE_ENV</code> and exports one block per environment.</p><p><code>database/index.js</code> is the Sequelize connection used by the running app server — it reads the same <code>DATABASE_URL</code>. Two separate entry points, same database.</p><hr><h2 id="running-locally" tabindex="-1">Running Locally <a class="header-anchor" href="#running-locally" aria-label="Permalink to &quot;Running Locally&quot;">​</a></h2><p>Orbital-Express has <strong>three processes</strong>. You need all three running during development:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">yarn</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> s</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      # web server (index.js → server.js, clustered via throng)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">yarn</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> w</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      # background worker (worker.js — processes Bull queue jobs)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">yarn</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> cron</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # cron daemon (cronjobs.js — enqueues scheduled work)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><h3 id="why-three-processes" tabindex="-1">Why three processes? <a class="header-anchor" href="#why-three-processes" aria-label="Permalink to &quot;Why three processes?&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Process</th><th>What it does</th></tr></thead><tbody><tr><td><strong>Web server</strong> (<code>yarn s</code>)</td><td>Handles all incoming HTTP and WebSocket requests.</td></tr><tr><td><strong>Worker</strong> (<code>yarn w</code>)</td><td>Runs background jobs — tasks that are too slow or heavy for the request/response cycle (report generation, third-party API calls, bulk operations).</td></tr><tr><td><strong>Cron</strong> (<code>yarn cron</code>)</td><td>A clock process — triggers scheduled work (e.g. &quot;send a daily email at noon&quot;). Typically enqueues a worker task rather than doing the work itself.</td></tr></tbody></table><p>The cron process should run as <strong>exactly one instance</strong> in production. The web server and worker can scale horizontally.</p><div class="tip custom-block"><p class="custom-block-title">Development shortcut</p><p>In local development you can open three terminal tabs, one per process. In production these are separate dynos/containers.</p></div><p>For the full command reference, database backup/restore, Heroku deploy steps, health probes, and ngrok webhook testing, see the <a href="./../reference/operations.html">Operations &amp; Deploy reference</a>.</p><hr><h2 id="your-first-migration" tabindex="-1">Your First Migration <a class="header-anchor" href="#your-first-migration" aria-label="Permalink to &quot;Your First Migration&quot;">​</a></h2><p>Once Postgres is running and your <code>config/.env.development</code> is configured:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">yarn</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> migrate</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>This applies all pending migrations in <code>migrations/</code> to your development database, in order. The Sequelize CLI reads <code>config/config.js</code>, which picks up <code>DATABASE_URL</code> from <code>.env.development</code>.</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">yarn</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> migrate:prod</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # run against production DATABASE_URL</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">yarn</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> rollback</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        # roll back the last migration (development)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><div class="warning custom-block"><p class="custom-block-title">Never edit schema.sql to change the database</p><p><code>database/schema.sql</code> is documentation — it is never executed. The real schema changes live in <code>migrations/</code>. Always create a migration file via:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sequelize</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> migration:create</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --name</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> descriptive-name</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Then record the same change in <code>database/schema.sql</code> so every developer can read the full schema in one place.</p></div><hr><h2 id="running-tests" tabindex="-1">Running Tests <a class="header-anchor" href="#running-tests" aria-label="Permalink to &quot;Running Tests&quot;">​</a></h2><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">yarn</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>The test command runs three things in sequence: <code>yarn lang</code> (compiles and validates i18n), applies test fixtures, then runs the full Jest suite with <code>--runInBand</code>.</p><div class="warning custom-block"><p class="custom-block-title">Postgres and Redis must be running</p><p>The integration tests hit a real Postgres database (pointed at by <code>DATABASE_URL</code> in <code>.env.test</code>) and a real Redis. Start both before running <code>yarn test</code>.</p></div><p>Test files mirror source file locations exactly:</p><ul><li>Feature action tests → <code>app/&lt;Feature&gt;/tests/integration/</code></li><li>Feature task tests → <code>app/&lt;Feature&gt;/tests/tasks/</code></li><li>Global helper tests → <code>test/helpers/&lt;name&gt;.test.js</code></li><li>Global service tests → <code>test/services/&lt;name&gt;.test.js</code></li></ul><hr><h2 id="project-layout-at-a-glance" tabindex="-1">Project Layout at a Glance <a class="header-anchor" href="#project-layout-at-a-glance" aria-label="Permalink to &quot;Project Layout at a Glance&quot;">​</a></h2><p>Almost everything you build day-to-day lives in <code>app/</code> — one folder per feature (one per database table). Everything else at the repo root is shared infrastructure.</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>repo root</span></span>
<span class="line"><span>├── app/                  # ALL features — one folder per feature (table)</span></span>
<span class="line"><span>├── index.js              # web entry point (clustered via throng)</span></span>
<span class="line"><span>├── server.js             # Express app: middleware, routes, socket, error handler</span></span>
<span class="line"><span>├── worker.js             # background worker — registers all queue task processors</span></span>
<span class="line"><span>├── cronjobs.js           # cron clock — schedules jobs</span></span>
<span class="line"><span>├── routes.js             # global route aggregator (mounts every feature&#39;s routes.js)</span></span>
<span class="line"><span>├── models.js             # global model aggregator (scans app/*/model.js)</span></span>
<span class="line"><span>├── middleware/           # global Express middleware (id, args, auth, error, exit)</span></span>
<span class="line"><span>├── services/             # third-party wrappers + shared infra (queue, redis, socket, email…)</span></span>
<span class="line"><span>├── helpers/              # small pure utility functions shared across features</span></span>
<span class="line"><span>├── database/             # schema.sql, sequence.js, seed/, backups/, index.js</span></span>
<span class="line"><span>├── migrations/           # ordered schema changes applied by yarn migrate</span></span>
<span class="line"><span>├── languages/            # global i18n source strings (compiled by yarn lang)</span></span>
<span class="line"><span>├── locales/              # compiled i18n output — never edit by hand</span></span>
<span class="line"><span>├── config/               # per-environment .env files + config glue</span></span>
<span class="line"><span>├── docs/                 # all deep documentation</span></span>
<span class="line"><span>└── test/                 # global test entry: fixtures, helper/service unit tests</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br></div></div><p>The <code>app/</code> folder is where you spend most of your time. Each feature folder holds everything related to that feature:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>app/</span></span>
<span class="line"><span>└── Order/</span></span>
<span class="line"><span>    ├── model.js</span></span>
<span class="line"><span>    ├── routes.js</span></span>
<span class="line"><span>    ├── controller.js</span></span>
<span class="line"><span>    ├── actions/</span></span>
<span class="line"><span>    │   ├── index.js</span></span>
<span class="line"><span>    │   ├── V1Create.js</span></span>
<span class="line"><span>    │   └── V1Get.js</span></span>
<span class="line"><span>    ├── tasks/</span></span>
<span class="line"><span>    ├── tests/</span></span>
<span class="line"><span>    ├── languages/</span></span>
<span class="line"><span>    └── mailers/</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br></div></div><hr><h2 id="scaffolding-your-first-feature" tabindex="-1">Scaffolding Your First Feature <a class="header-anchor" href="#scaffolding-your-first-feature" aria-label="Permalink to &quot;Scaffolding Your First Feature&quot;">​</a></h2><p>The generator is the <strong>only</strong> way to create new feature files. Never create them by hand.</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">yarn</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gen</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> Order</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                    # scaffold the Order feature folder</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">yarn</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gen</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> Order</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -a</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> V1Create</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        # add an action to an existing feature</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">yarn</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gen</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> Order</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -t</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> V1ProcessTask</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # add a background task</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">yarn</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gen</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> Order</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -m</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> OrderMailer</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     # add a mailer</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>After scaffolding, immediately remove the generator&#39;s default placeholder files:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">yarn</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> del</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> Order</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -a</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> V1Example</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">       # removes the placeholder action + its export entry</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><div class="warning custom-block"><p class="custom-block-title">Use yarn del, not rm</p><p><code>yarn del</code> removes the file <strong>and</strong> cleans up its entry in <code>actions/index.js</code> (or <code>tasks/index.js</code>). Using <code>rm</code> directly leaves a broken export pointing at a deleted file.</p></div><p>After generating, run <code>yarn lang</code> to compile i18n, then <code>yarn test</code> to confirm the scaffold passes the suite.</p><hr><h2 id="next-steps" tabindex="-1">Next Steps <a class="header-anchor" href="#next-steps" aria-label="Permalink to &quot;Next Steps&quot;">​</a></h2><ul><li><a href="./project-structure.html">Project Structure</a> — every folder explained in depth.</li><li><a href="/orbital-express/core/feature-folder.html">Feature-Based Development</a> — how to build a feature end-to-end.</li><li><a href="/orbital-express/auth/overview.html">Authentication</a> — the two-token model, session management, and adding new user types.</li><li><a href="/orbital-express/background-jobs/overview.html">Background Jobs &amp; Cron</a> — tasks, queues, and scheduled work.</li><li><a href="/orbital-express/testing/overview.html">Testing</a> — fixtures, integration tests, and what to assert.</li><li><a href="../conventions.txt"><code>docs/conventions.txt</code></a> — the authoritative rulebook. When anything is ambiguous, this wins.</li></ul>`,76)])])}const g=s(t,[["render",r]]);export{u as __pageData,g as default};
